cat > ~/vtu-app/package.json << 'EOF'
{
  "name": "vtu-hub",
  "version": "1.0.0",
  "description": "VTU App with Firebase",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase": "^10.11.0"
  }
}
EOF

